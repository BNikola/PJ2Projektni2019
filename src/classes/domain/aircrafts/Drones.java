package classes.domain.aircrafts;

public class Drones extends Aircraft {

    public Drones(String aircraftId, boolean foreign, double height, String model, int speed) {
        super(aircraftId, foreign, height, model, speed);
    }
}
